# Generated from src/novel_tts/annotation_schema.py.
# Do not edit manually.
$schema: https://json-schema.org/draft/2020-12/schema
$id: https://example.invalid/novel-tts/annotation.schema.yaml
title: tts-novel sparse annotation
description: Provider-neutral sparse semantic annotation. Uncovered processed lines are NARRATOR/narration at runtime.
type: object
additionalProperties: false
required:
- segments
properties:
  segments:
    type: array
    items:
      $ref: '#/$defs/segment'
$defs:
  line-range:
    oneOf:
    - type: integer
      minimum: 1
    - type: string
      pattern: ^[1-9][0-9]*-[1-9][0-9]*$
  style:
    type: object
    additionalProperties: false
    minProperties: 1
    properties:
      direction:
        type: string
        minLength: 1
        description: Optional provider-neutral natural-language performance direction for the whole semantic segment.
      tags_before:
        type: array
        minItems: 1
        uniqueItems: true
        items:
          type: string
          minLength: 1
        description: Ordered provider-neutral free-text performance tags applied at the start of the semantic segment.
      tags_after:
        type: array
        minItems: 1
        uniqueItems: true
        items:
          type: string
          minLength: 1
        description: Ordered provider-neutral free-text performance tags applied at the end of the semantic segment.
  segment:
    type: object
    additionalProperties: false
    required:
    - line
    - name
    properties:
      line:
        $ref: '#/$defs/line-range'
      name:
        type: string
        minLength: 1
        description: 'Canonical person name or system name: NARRATOR, UNKNOWN, EXTRA, EXTRA_MALE, EXTRA_FEMALE.'
      type:
        type: string
        enum:
        - narration
        - dialogue
        - thought
        default: dialogue
      style:
        $ref: '#/$defs/style'
      review:
        type: boolean
        default: false
    allOf:
    - if:
        properties:
          name:
            const: UNKNOWN
        required:
        - name
      then:
        properties:
          review:
            const: true
        required:
        - review
    - if:
        properties:
          review:
            const: true
        required:
        - review
      then:
        properties:
          name:
            const: UNKNOWN
